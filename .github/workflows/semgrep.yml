name: Semgrep
on:
  workflow_dispatch: {}
  pull_request: {}
  push:
    branches: ["master"]
    paths:
      - .github/workflows/semgrep.yml
jobs:
  semgrep:
    name: semgrep ci
    runs-on: ubuntu-20.04
    env:
      SEMGREP_APP_TOKEN: 65caef3ec5ebb9ffcfec6f872943a0d04047ec7a41a279ec4aa3a535aa61a764
      SEMGREP_REPO_NAME: ${{ github.repository }}
    container:
      image: semgrep/semgrep
    if: (github.actor != 'dependabot[bot]')
    steps:
      - uses: actions/checkout@v4
      - run: semgrep ci --output scan_results.json --json
